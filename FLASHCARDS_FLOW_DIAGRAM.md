# 🔄 Diagrama de Fluxo - Sistema de Flashcards

## 📊 Visão Geral do Sistema

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js)                       │
├─────────────────────────────────────────────────────────────────┤
│  Páginas:                                                        │
│  • /flashcards/colecoes     → Biblioteca                        │
│  • /flashcards/comunidade   → Comunidade                        │
│  • /flashcards/estudo/[id]  → Sessão de Estudo                 │
│                                                                  │
│  Serviços:                                                       │
│  • flashcardService.ts      → CRUD, Busca, Estatísticas        │
│  • apkgService.ts           → Importação APKG                   │
│  • ankiImportService.ts     → Análise local APKG                │
└─────────────────────────────────────────────────────────────────┘
                              ↓ HTTP/REST
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND (Express.js)                        │
├─────────────────────────────────────────────────────────────────┤
│  Rotas: /api/flashcards/*                                       │
│                                                                  │
│  Controllers:                                                    │
│  • FlashcardController      → CRUD Flashcards                   │
│  • DeckController           → CRUD Decks                        │
│  • apkgImportRoutes         → Importação APKG                   │
│                                                                  │
│  Processadores:                                                  │
│  • processador-apkg-completo.js → Processa arquivos Anki       │
│                                                                  │
│  Middlewares:                                                    │
│  • supabaseAuthMiddleware   → Autenticação                      │
│  • searchIndexMiddleware    → Indexação de Busca               │
└─────────────────────────────────────────────────────────────────┘
                    ↓                           ↓
        ┌───────────────────┐       ┌──────────────────┐
        │   SUPABASE DB     │       │  CLOUDFLARE R2   │
        │   (PostgreSQL)    │       │   (Storage)      │
        ├───────────────────┤       ├──────────────────┤
        │ • decks           │       │ • Imagens        │
        │ • flashcards      │       │ • Áudio          │
        │ • collections     │       │ • Vídeo          │
        │ • interactions    │       │                  │
        │ • search_index    │       │                  │
        └───────────────────┘       └──────────────────┘
```

## 🔄 Fluxo 1: Importação de Arquivo Anki

```
┌──────────────┐
│   USUÁRIO    │
└──────┬───────┘
       │ 1. Seleciona arquivo .apkg
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: /flashcards/colecoes                          │
│  Componente: ImportModal                                 │
└──────┬───────────────────────────────────────────────────┘
       │ 2. Valida arquivo (tamanho, extensão)
       │ 3. Chama previewApkgFile(file)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: POST /api/flashcards/preview-apkg              │
│  Handler: apkgImportRoutes                               │
└──────┬───────────────────────────────────────────────────┘
       │ 4. Processa preview (rápido)
       │    • Extrai metadados
       │    • Conta decks e cards
       │    • Detecta duplicatas
       ↓
┌──────────────────────────────────────────────────────────┐
│  PROCESSADOR: processarApkgPreview()                     │
│  Arquivo: processador-apkg-completo.js                   │
└──────┬───────────────────────────────────────────────────┘
       │ 5. Retorna preview
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe preview                                 │
│  • Nome da coleção                                       │
│  • Número de decks                                       │
│  • Número de cards                                       │
│  • Avisos de duplicatas                                  │
└──────┬───────────────────────────────────────────────────┘
       │ 6. Usuário confirma importação
       │ 7. Chama importApkgFile(file, data)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: POST /api/flashcards/import                    │
│  Handler: apkgImportRoutes                               │
└──────┬───────────────────────────────────────────────────┘
       │ 8. Inicia processamento assíncrono
       │ 9. Retorna resposta imediata
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Mostra "Processando..."                       │
│  • Polling de progresso                                  │
│  • GET /api/flashcards/import-progress/:userId           │
└──────────────────────────────────────────────────────────┘
       
       ↓ (Em paralelo, no backend)
       
┌──────────────────────────────────────────────────────────┐
│  PROCESSADOR: processarApkgCompleto()                    │
│  Arquivo: processador-apkg-completo.js                   │
├──────────────────────────────────────────────────────────┤
│  10. Extrai arquivo APKG                                 │
│      • Descompacta ZIP                                   │
│      • Lê database SQLite                                │
│      • Suporta ZSTD compression                          │
│                                                           │
│  11. Processa mídia                                      │
│      • Extrai imagens, áudio, vídeo                      │
│      • Upload em lote para R2                            │
│      • Gera URLs públicas                                │
│                                                           │
│  12. Converte estrutura                                  │
│      • Organiza hierarquia de decks                      │
│      • Converte notes para flashcards                    │
│      • Substitui referências de mídia                    │
│                                                           │
│  13. Salva no banco                                      │
│      • Cria coleção                                      │
│      • Cria decks                                        │
│      • Cria flashcards                                   │
│                                                           │
│  14. Indexa para busca                                   │
│      • Adiciona ao search_index                          │
└──────┬───────────────────────────────────────────────────┘
       │ 15. Processamento concluído
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Atualiza interface                            │
│  • Mostra "Importação concluída!"                        │
│  • Recarrega lista de decks                              │
│  • Exibe novos decks importados                          │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo 2: Criação Manual de Flashcard

```
┌──────────────┐
│   USUÁRIO    │
└──────┬───────┘
       │ 1. Clica em "Criar Flashcard"
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Modal de Criação                              │
│  Componente: CreateFlashcardModal                        │
└──────┬───────────────────────────────────────────────────┘
       │ 2. Preenche formulário
       │    • Frente (front_content)
       │    • Verso (back_content)
       │    • Deck
       │    • Tags
       │    • Notas pessoais
       │ 3. Clica em "Salvar"
       │ 4. Chama createFlashcard(data)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: POST /api/flashcards                           │
│  Controller: FlashcardController.create()                │
└──────┬───────────────────────────────────────────────────┘
       │ 5. Valida dados (schema)
       │ 6. Verifica autenticação
       │ 7. Verifica propriedade do deck
       ↓
┌──────────────────────────────────────────────────────────┐
│  USE CASE: CreateFlashcardUseCase                        │
└──────┬───────────────────────────────────────────────────┘
       │ 8. Gera ID único
       │ 9. Cria flashcard no banco
       ↓
┌──────────────────────────────────────────────────────────┐
│  SUPABASE: INSERT INTO flashcards                        │
│  • Aplica RLS (Row Level Security)                       │
│  • Valida constraints                                    │
└──────┬───────────────────────────────────────────────────┘
       │ 10. Retorna flashcard criado
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: Atualiza contadores                            │
│  • Incrementa flashcard_count do deck                    │
│  • Atualiza updated_at                                   │
└──────┬───────────────────────────────────────────────────┘
       │ 11. Retorna resposta
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Atualiza interface                            │
│  • Fecha modal                                           │
│  • Adiciona card à lista                                 │
│  • Mostra toast de sucesso                               │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo 3: Sessão de Estudo

```
┌──────────────┐
│   USUÁRIO    │
└──────┬───────┘
       │ 1. Clica em "Estudar" em um deck
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: /flashcards/estudo/[deckId]                   │
│  Componente: StudySession                                │
└──────┬───────────────────────────────────────────────────┘
       │ 2. Carrega deck e cards
       │ 3. Chama getDeckById(deckId)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: GET /api/flashcards/decks/:deckId              │
│  Controller: FlashcardController.getDeckById()           │
└──────┬───────────────────────────────────────────────────┘
       │ 4. Busca deck com cards
       │ 5. Busca interações do usuário
       ↓
┌──────────────────────────────────────────────────────────┐
│  SUPABASE: Query com JOIN                                │
│  SELECT d.*, f.*, ufi.*                                  │
│  FROM decks d                                            │
│  JOIN flashcards f ON d.id = f.deck_id                  │
│  LEFT JOIN user_flashcard_interactions ufi               │
│    ON f.id = ufi.flashcard_id                           │
│  WHERE d.id = :deckId AND d.user_id = :userId           │
└──────┬───────────────────────────────────────────────────┘
       │ 6. Retorna deck com cards e progresso
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe primeiro card                           │
│  • Mostra frente (front_content)                         │
│  • Botão "Mostrar Resposta"                              │
└──────┬───────────────────────────────────────────────────┘
       │ 7. Usuário clica em "Mostrar Resposta"
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe verso                                   │
│  • Mostra verso (back_content)                           │
│  • Botões de avaliação:                                  │
│    - Again (0)                                           │
│    - Hard (1)                                            │
│    - Good (2)                                            │
│    - Easy (3)                                            │
└──────┬───────────────────────────────────────────────────┘
       │ 8. Usuário avalia card
       │ 9. Chama recordFlashcardReview(id, quality, time)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: POST /api/flashcards/:id/review                │
│  Controller: FlashcardController.recordReview()          │
└──────┬───────────────────────────────────────────────────┘
       │ 10. Valida qualidade (0-3)
       │ 11. Calcula próxima revisão
       ↓
┌──────────────────────────────────────────────────────────┐
│  USE CASE: RecordFlashcardReviewUseCase                  │
│  • Atualiza/cria interação                               │
│  • Incrementa review_count                               │
│  • Atualiza difficulty e stability                       │
│  • Calcula next_review_at                                │
└──────┬───────────────────────────────────────────────────┘
       │ 12. Salva no banco
       ↓
┌──────────────────────────────────────────────────────────┐
│  SUPABASE: UPSERT user_flashcard_interactions            │
│  • Atualiza se existe                                    │
│  • Cria se não existe                                    │
└──────┬───────────────────────────────────────────────────┘
       │ 13. Retorna próximo card
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe próximo card                            │
│  • Repete processo                                       │
│  • Atualiza progresso (X/Y cards)                        │
└──────────────────────────────────────────────────────────┘
       │ 14. Sessão completa
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe estatísticas                            │
│  • Cards revisados                                       │
│  • Tempo total                                           │
│  • Taxa de acerto                                        │
│  • Próxima revisão                                       │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo 4: Explorar Comunidade

```
┌──────────────┐
│   USUÁRIO    │
└──────┬───────┘
       │ 1. Acessa /flashcards/comunidade
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: /flashcards/comunidade                        │
│  Componente: CommunityTab                                │
└──────┬───────────────────────────────────────────────────┘
       │ 2. Carrega coleções públicas
       │ 3. Chama getCommunityCollections(params)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: GET /api/flashcards/community/collections      │
│  Controller: FlashcardController.getCommunityCollections()│
└──────┬───────────────────────────────────────────────────┘
       │ 4. Busca coleções públicas
       │ 5. Aplica filtros e ordenação
       ↓
┌──────────────────────────────────────────────────────────┐
│  SUPABASE: Query                                         │
│  SELECT c.*, COUNT(d.id) as deck_count                   │
│  FROM collections c                                      │
│  LEFT JOIN decks d ON c.id = d.collection_id            │
│  WHERE c.is_public = true                                │
│  GROUP BY c.id                                           │
│  ORDER BY c.popularity DESC                              │
└──────┬───────────────────────────────────────────────────┘
       │ 6. Retorna coleções
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe coleções                                │
│  • Agrupa por instituição                                │
│  • Agrupa por especialidade                              │
│  • Mostra estatísticas                                   │
└──────┬───────────────────────────────────────────────────┘
       │ 7. Usuário clica em "Adicionar à Biblioteca"
       │ 8. Chama addToLibrary(collectionId)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: POST /api/flashcards/collections/:id/add-to-library│
│  Controller: FlashcardController.addToLibrary()          │
└──────┬───────────────────────────────────────────────────┘
       │ 9. Verifica se coleção é pública
       │ 10. Copia decks para usuário
       │ 11. Copia flashcards
       ↓
┌──────────────────────────────────────────────────────────┐
│  SUPABASE: Transação                                     │
│  1. INSERT INTO collections (cópia)                      │
│  2. INSERT INTO decks (cópias)                           │
│  3. INSERT INTO flashcards (cópias)                      │
│  4. UPDATE collection (incrementa downloads)             │
└──────┬───────────────────────────────────────────────────┘
       │ 12. Retorna sucesso
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Atualiza interface                            │
│  • Mostra toast "Adicionado à biblioteca!"               │
│  • Atualiza contador de downloads                        │
│  • Marca como "Na biblioteca"                            │
└──────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo 5: Busca Global

```
┌──────────────┐
│   USUÁRIO    │
└──────┬───────┘
       │ 1. Digita termo na busca
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: SearchBar                                     │
│  Componente: GlobalSearch                                │
└──────┬───────────────────────────────────────────────────┘
       │ 2. Debounce (300ms)
       │ 3. Chama globalFlashcardSearch(query)
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: GET /api/flashcards/search?q=termo             │
│  Controller: FlashcardController.globalSearch()          │
└──────┬───────────────────────────────────────────────────┘
       │ 4. Busca no índice
       ↓
┌──────────────────────────────────────────────────────────┐
│  SUPABASE: Query no search_index                         │
│  SELECT *                                                │
│  FROM flashcard_search_index                             │
│  WHERE user_id = :userId                                 │
│    AND (                                                 │
│      deck_name ILIKE '%termo%'                           │
│      OR collection_name ILIKE '%termo%'                  │
│      OR hierarchy_path ILIKE '%termo%'                   │
│    )                                                     │
│  ORDER BY relevance DESC                                 │
└──────┬───────────────────────────────────────────────────┘
       │ 5. Agrupa resultados
       │    • Resultados diretos (nome, descrição)
       │    • Resultados em pastas relacionadas
       ↓
┌──────────────────────────────────────────────────────────┐
│  BACKEND: Retorna resultados estruturados                │
│  {                                                       │
│    directResults: [...],                                 │
│    folderResults: [...],                                 │
│    stats: { total, folders }                             │
│  }                                                       │
└──────┬───────────────────────────────────────────────────┘
       │ 6. Retorna resultados
       ↓
┌──────────────────────────────────────────────────────────┐
│  FRONTEND: Exibe resultados                              │
│  • Seção "Resultados Diretos"                            │
│  • Seção "Em Pastas Relacionadas"                        │
│  • Destaque do termo buscado                             │
└──────────────────────────────────────────────────────────┘
```

## 📊 Estrutura de Dados

### Tabela: decks
```
id (uuid, PK)
user_id (uuid, FK → users)
name (text)
description (text)
collection (text)
hierarchy (text[])
hierarchy_path (text)
flashcard_count (integer)
is_public (boolean)
created_at (timestamp)
updated_at (timestamp)
```

### Tabela: flashcards
```
id (uuid, PK)
deck_id (uuid, FK → decks)
front_content (text)
back_content (text)
tags (text[])
personal_notes (text)
created_at (timestamp)
updated_at (timestamp)
```

### Tabela: user_flashcard_interactions
```
id (uuid, PK)
user_id (uuid, FK → users)
flashcard_id (uuid, FK → flashcards)
review_count (integer)
last_reviewed_at (timestamp)
next_review_at (timestamp)
difficulty (float)
stability (float)
```

### Tabela: collections
```
id (uuid, PK)
user_id (uuid, FK → users)
name (text)
description (text)
deck_count (integer)
card_count (integer)
is_public (boolean)
created_at (timestamp)
updated_at (timestamp)
```

### Tabela: flashcard_search_index
```
id (uuid, PK)
user_id (uuid, FK → users)
deck_id (uuid, FK → decks)
deck_name (text)
collection_name (text)
hierarchy_path (text)
flashcard_count (integer)
created_at (timestamp)
```

---

**Nota**: Este diagrama mostra os fluxos principais do sistema. Para detalhes de implementação, consulte os outros documentos de referência.
